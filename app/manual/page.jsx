import Manual from "@/componentes/Manual";

export const metadata = {
  title: "Manual · Pauta",
  description: "Como a Pauta funciona: a grade, as cores, os status, os relatórios e o que fazer quando algo não bate.",
};

export default function PaginaManual() {
  return <Manual />;
}
